{
  "name": "saas-mailer-server",
  "version": "0.1.0",
  "private": true,
  "main": "api.js",
  "scripts": {
    "start": "node api.js",
    "dev": "nodemon api.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:generate-dev": "prisma generate --schema=../prisma/schema.dev.prisma",
    "prisma:migrate-dev": "prisma migrate dev --schema=../prisma/schema.dev.prisma --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "body-parser": "^1.20.0",
    "bullmq": "^2.0.0",
    "express": "^4.18.2",
    "googleapis": "^115.0.0",
    "ioredis": "^5.0.0",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "nodemon": "^2.0.22"
  }
}
